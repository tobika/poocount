angular.module('starter.controllers').controller('StatsController', function($scope, Database, $timeout, $translate, SettingsService) {

    $scope.$on("$ionicView.beforeEnter", function( scopes, states ) {
        //alert('fuh');
    });
});